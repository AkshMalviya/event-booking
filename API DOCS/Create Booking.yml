info:
  name: Create Booking
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/bookings
  body:
    type: json
    data: |-
      {
        "eventId": "6a9ffc6e332989f10aef33d8",
        "ticketsCount": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
